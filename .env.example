# ──────────────────────────────────────────────────────────────────────────────
# Pace Pilot — Environment Variables
# ──────────────────────────────────────────────────────────────────────────────
#
# Copy this file to .env (or .env.local) and fill in your values.
# Never commit .env to version control.
#
# All variables exposed to the browser MUST start with VITE_ (Vite convention).
# ──────────────────────────────────────────────────────────────────────────────

# ── Google Gemini AI ──────────────────────────────────────────────────────────
# Obtain at: https://aistudio.google.com/app/apikey
# ⚠  This key is embedded in the client bundle. Use a backend proxy in production.
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ── Appwrite ──────────────────────────────────────────────────────────────────
# Create a free project at: https://cloud.appwrite.io
#
# Leave VITE_APPWRITE_PROJECT_ID empty to run in mock-data / demo mode
# (no Appwrite account required for local development).

VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=your_project_id_here
VITE_APPWRITE_DATABASE_ID=your_database_id_here

# Collection IDs (create these in your Appwrite Console — see README)
VITE_APPWRITE_TASKS_COLLECTION_ID=tasks
VITE_APPWRITE_PROJECTS_COLLECTION_ID=projects
VITE_APPWRITE_CALENDAR_COLLECTION_ID=calendar_events
VITE_APPWRITE_RECURRING_COLLECTION_ID=recurring_tasks
VITE_APPWRITE_REPORTS_COLLECTION_ID=daily_reports
