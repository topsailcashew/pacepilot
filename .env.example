# ──────────────────────────────────────────────
# Pace Pilot — Environment Variables
# ──────────────────────────────────────────────
#
# Copy this file to .env (or .env.local) and fill in your values.
# Never commit .env to version control.
#
# All variables exposed to the browser MUST start with VITE_
# (Vite convention) — they are embedded into the built bundle.
#
# ⚠  Security note: Gemini API calls are made client-side in this prototype.
#    For production, proxy them through a backend to keep your key secret.
# ──────────────────────────────────────────────

# Google Gemini API key — obtain at https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here
